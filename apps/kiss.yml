services:
  kiss:
    image: ghcr.io/caslus/kiss:latest
    container_name: kiss
    ports:
      - "8080:8080"
    networks:
      - homelab_network
    volumes:
      - ${DATA}/kiss:/app/external
    restart: unless-stopped